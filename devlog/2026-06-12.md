# 2026-06-12

## 本次工作

- [x] 初始化 Git 仓库，创建 `README.md` 与 `.gitignore`。
- [x] 将默认分支设置为 `main`，远程仓库设置为 `https://github.com/2553120912/habitasdesign.git`。
- [x] 压缩首页背景视频：
  - 原始文件保留为 `videos/hero-original-large.mp4`。
  - 网页继续使用压缩后的 `videos/hero.mp4`。
  - 原始大视频已加入 `.gitignore`，避免超过 GitHub 100 MB 单文件限制。
- [x] 修复侧栏导航当前页下划线：
  - `projects.html` 与项目详情页停留在“作品”。
  - `process.html` 停留在“服务流程”。
  - `contact.html` 停留在“联系我们”。
  - 首页不再默认显示“关于我们”常驻下划线。
- [x] 修复首页打开时可能自动下滑的问题：
  - 直接打开或刷新首页时强制回到顶部。
  - 从其他页面点击“关于我们”时仍然允许跳转到 About 区域。

## GitHub 上传状态

- [x] 本地已创建首次提交。
- [x] 本地已提交侧栏导航修复。
- [x] 本地已提交首页初始滚动修复。
- [ ] 远程推送暂未完成。

## 当前阻塞

- `git push -u origin main` 多次失败，错误为无法连接 `github.com:443`。
- 判断为当前网络或 Git 代理未连通 GitHub，不是仓库内容问题。

## 后续

- 网络或代理恢复后，重新执行：

```powershell
git push -u origin main
```

- 若使用本地代理，需要确认 Git 能走对应代理端口。
