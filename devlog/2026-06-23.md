# 2026-06-23 开发日志

## 今日完成

- [x] 重新引用 Process 图片资源
  - `process.html` 顶部图使用 `images/process/1.png`
  - 首页 Process 轮播改为读取 `images/process/`
  - 当前轮播先使用 `1.png`，后续新增图片可继续按数字追加

- [x] 优化 Process 页面英文标题排版
  - 将 intro 标题拆成 eyebrow + 主标题两级结构
  - 英文改为 `Seamless · Assured · Complete` / `A guided journey home`
  - 中文同步拆为 `无隙 · 无忧 · 无缺` / `打造伴随式筑家之旅`
  - 调整左右栏比例为约 `45% / 45%`，中间保留约 `10%` 空档
  - 左侧标题与右侧正文改为顶部对齐，减少视觉高度差

- [x] 新增 About Us 页 Team 展示区
  - 复用 `story.html` 现有 About Us / Team tab
  - 点击 Team 后隐藏 About Us 的轮播图与文字，只显示团队网格
  - 新增 7 个团队成员占位卡片，姓名与职位均为待定
  - 桌面端采用错落式两列布局，右列整体向下偏移形成交错节奏
  - 移动端改为单列堆叠，避免横向溢出
  - 人物图片占位框最终调整为正方形

- [x] 细化 About Us 页 Team 卡片排版
  - 保留 About Us / Team tab 切换，并补强隐藏 panel 的 CSS 规则
  - Team 区移除上方轮播与文案，仅显示团队成员网格
  - 调整第 3、4 个成员卡为同排右侧紧凑并列，保持与第 1、2 个相同尺寸
  - 第 5 个成员卡单独成排，向右轻微偏移，并统一与上一排的垂直间距
  - 第 6、7 个成员卡调整为最后一排并列，移动端仍按 1-7 顺序单列堆叠

- [x] Team 图片框放大（稳妥方案，留白不压缩）
  - `story.html` 的 `--team-card-size` 由 `clamp(280px, 34vw, 520px)` 改为 `clamp(320px, 38vw, 728px)`，上限 ×1.4
  - `.team-grid` 容器 `max-width` 由 `1500px` 加宽到 `1820px`，保证两列大图框 + 现有列间/行间留白不被挤压
  - 各间距值与断点保持不变，宽屏放大最明显，普通笔记本放大幅度略小（避免横向溢出）

- [x] 新增团队成员详情面板（点击卡片右侧滑入）
  - 7 张真实卡片加 `data-member="01"~"07"` + 键盘可达（tabindex/role）
  - 复用导航侧边栏「遮罩 + 滑入 + 锁滚动 + Esc 关闭」模式，新增 `.member-overlay` / `.member-panel`
  - 面板自上而下：人物大图（复制被点卡片背景，占位/真实图通用）→ 引述（衬线大字带装饰引号）→ 细分隔线 → 姓名/职位/学历 → 多段简介 → 右上角 CLOSE
  - 接入现有 i18n：打开时动态改面板 data-i18n 键并调 `applyLang()`，点谁显示谁、语言切换同步；新增 `team-quote/edu/bio-0X` 双语占位键
  - 面板内容独立滚动；点遮罩 / CLOSE / Esc 关闭；移动端面板占满全屏
  - 顶部大图最终定为正方形、宽度为面板的一半

- [x] 全站「故事」入口改为「团队」并指向 Team 标签
  - 首页 CTA「我们的故事 / OUR STORY」改为「我们的团队 / OUR TEAM」，链接 `story.html#team`
  - 全站 8 个页面侧栏 `nav-stories` 文案改「团队 / Team」，链接 `story.html#team`
  - `story.html` 新增 `#team` 处理：进入或 hash 变化时自动切到 Team 标签（复用现有 tab 点击逻辑）
  - `home-v2.html`（首页试验版）不在范围内，未改动

- [x] 「返回首页」按钮扩展到 Projects 与 About Us 页底部
  - 复用 process.html 的 `.process-home-link` 按钮（圆形右箭头、hover 下划线展开 + 图标右移）
  - `projects.html`：项目网格与深色 footer 之间新增居中收尾区 `.projects-closing`
  - `story.html`：两个标签下方、footer 之上新增居中收尾区 `.story-closing`
  - 两页各补 `back-home` 双语键（返回首页 / Back to Home），链接 `index.html`

## 当前状态

- Process 页面：英文 intro 层级更清晰，左右栏排版更平衡
- 首页 Process 区块：图片路径已统一到 `images/process/`
- About Us 页面：Team tab 已可用，当前为 7 个占位成员，桌面端卡片位置已按分行节奏细化
- Team 卡片图框已放大、留白保持；点击任一成员可从右侧滑出详情面板
- 全站「团队」入口（首页 CTA + 侧栏）已统一指向 `story.html#team`，落地即显示 Team 标签

## 后续

- [ ] Team 成员真实图片、姓名、职位确认后替换占位内容
- [ ] Team 详情面板的引述 / 学历 / 简介文案待用户中英双语填写（字典键 `team-quote/edu/bio-0X`）
- [ ] 如继续新增 Process 轮播图，可放入 `images/process/` 并在 `index.html` 图片数组中追加文件名
- [ ] Team 页面如后续需要独立入口，可再评估是否拆成单独 `team.html`
