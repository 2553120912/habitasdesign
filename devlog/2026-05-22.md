# 2026-05-22 开发日志

## 今日完成

### Projects 二级页
- [x] 新增 `projects.html`，作为项目集合页。
- [x] 首页 `View Projects / 查看项目` 和作品封面图链接到 `projects.html`。
- [x] 首页侧栏“作品 / Projects”链接到 `projects.html`。
- [x] 首页作品图 hover 浮层去掉 `View / 查看`，只保留项目名和类型年份。
- [x] 项目页首版只展示真实项目 `Lovell House`，使用 `images/lovell-house.jpg`。

### Contact 二级页
- [x] 新增 `contact.html`。
- [x] 页面分为黑底联系方式区和白底表单区。
- [x] 黑底区改为左右结构：左侧 `CONTACT` 和说明文字，右侧联系方式。
- [x] 白底表单区改为左右结构：左侧 `PROJECT ENQUIRY` + 说明 + 无箭头自动轮播，右侧信息输入表单。
- [x] 表单字段包含：名、姓、手机、邮箱、预算、留言。
- [x] 表单为静态交互：提交后显示成功提示，不发请求、不打开邮箱客户端。
- [x] Contact 页面复用首页最新侧栏渐入样式和语言切换逻辑。
- [x] 首页侧栏“联系我们 / Contact”链接到 `contact.html`。

### Contact 表单细节
- [x] `Project Enquiry` 下方加入无箭头自动轮播。
- [x] 轮播复用首页 process 的视觉：`4 / 3` 比例、Lovell House 图、两个灰色 placeholder、5 秒自动切换。
- [x] 表单栏位整体压缩，减少输入框内部空白。
- [x] 调整 message 字段伸展方式，让 `Message` 标题和输入文字起点距离与其他字段一致。
- [x] 重排表单区上下对齐：右侧 `名 / First Name` 与左侧 `PROJECT ENQUIRY` 同高，提交区与左侧图片底部对齐。
- [x] 放大表单区轮播图，使其接近首页 process 左侧轮播的视觉占比。

### 侧栏动画与滚动条稳定
- [x] 侧栏打开方式从滑出改为原地渐入。
- [x] 逐步调整渐入时长，最终首页使用 1.5 秒 keyframes 渐入。
- [x] 增加 `scrollbar-gutter: stable` 和 `overflow-y: scroll`，避免打开侧栏时页面因滚动条变化而抖动。
- [x] 去掉早期 JS 动态 padding 补偿方案，改用 CSS 稳定滚动条槽位。
- [x] 首页侧栏文字增加轻微延迟淡入，改善动画体感。

### 首页内容与 CTA
- [x] 首页 About Us 两段文案替换为新的品牌故事文案，并补充中文翻译。
- [x] 首页 `OUR STORY` 文案改为大写，并将箭头改为与 `OUR PROCESS` 一致的圆形 CTA 样式。
- [x] 圆形 CTA 内箭头多轮修正：从 SVG 放大、位移微调、文本箭头，最终改为几何中心对齐 SVG。
- [x] 当前圆形 CTA 箭头总跨度为 `x=6` 到 `x=18`，中心点落在 viewBox `x=12`，与圆心对齐。
- [x] 保持圆形外框 `20px × 20px` 不变。

## 当前页面状态

- `index.html`
  - 首页已有 Hero、process 轮播、story、works、about、contact sections。
  - About 文案已更新。
  - 首页侧栏 `Contact` 指向 `contact.html`，`Projects` 指向 `projects.html`。
  - 圆形 CTA 箭头使用几何中心对齐 SVG。

- `projects.html`
  - 已有项目集合页首版。
  - 仍未同步首页最新侧栏 1.5 秒 keyframes 动画和部分导航调整。

- `contact.html`
  - 已有完整 Contact 二级页。
  - 表单为静态 UI，未接后端。
  - 表单区布局已多轮微调，当前重点是桌面端左右栏对齐。

## 待办事项

- [ ] 将首页最新侧栏动画、CTA 箭头、导航链接同步到 `projects.html`。
- [ ] 后续如有真实项目素材，扩展 `projects.html` 项目列表和作品详情页。
- [ ] Contact 表单如需真实提交，后续接入后端、表单服务或邮件 API。
- [ ] Lovell House 项目正式文案仍待补充。
- [ ] 首页 `process` 和 Contact 轮播中的两个灰色 placeholder 后续可替换为真实图片。

## 备注

- 今日主要工作集中在二级页搭建、联系页排版、CTA 细节、侧栏动画和文案替换。
- 圆形 CTA 箭头问题的最终结论：字形或普通右箭头 SVG 都容易产生视觉偏移，最稳定方案是自定义 SVG 路径，并让箭头总长度中心点与 viewBox 中心一致。
- Contact 表单底部对齐通过右侧表单 `height: 100%` 和 message 字段伸展实现，移动端恢复自然高度，避免产生大块空白。
