# 2026-07-03 开发日志

## 今日完成

- [x] 首页 Works 作品区：由「轮播」改为「左大图 + 右 2×2 小图」静态画廊
  - `.works-feature-inner` 改为竖向堆叠：上方保留标题/介绍/CTA，下方新增 `.works-gallery`
  - 左侧 `.works-card--feature` 大图 + 右侧 `.works-gallery-grid`（2×2 四格）
  - 3 张真实项目：Lovell House（大图）、Myrtle House、Knutsford House，图片仍取 `images/<项目>/精选卡/`
  - 2 个占位项目（Project 04 / 05）：灰块 + "Coming Soon"，HTML 里用醒目注释标出，方便日后替换
  - 移除原轮播 JS（`worksProjects` 数组、`fillCard`、`renderWorksCarousel`、点击换图监听及 `applyLang` 内调用）
  - 配色完全不变（米白背景、深色字、灰占位块 `#e8e6e1`）

- [x] Works 画廊排版对齐参考图（微调）
  - 图片间距 32px → 44px，更舒展
  - 每张图下方信息精简为「名字 + 年份」，去掉 COMPLETED 小标签与 `Residence ·` 前缀
  - 标题/信息包进 `.works-card-meta`，收紧行距（gap 4px、行高压紧）
  - 大图 `flex:1` 撑满右侧 2×2 整体高度 + 文字块等高，使左大图底边与右下小图底边对齐
  - 移动端画廊回落为单列（大图用固定 `aspect-ratio`，`flex:none`）

- [x] 新增「箭头 CTA 按钮」设计标准 → `docs/design-spec.md`
  - 以 Hero「Start Your Journey」为完美样板，抽象成与具体按钮无关的通用规则
  - 核心：以文字 `font-size` 为唯一基准，圆圈/箭头/下划线全部用 `em` 随字体缩放；箭头柄中心对齐圆圈中心
  - 比例：圆圈 `1.45em`、箭头 `0.9em`、间距 `0.55em`、下划线右缩进 `2em`、底距 `-0.09em`；附动效、颜色、参考 CSS/SVG
  - 仅写文档，未改按钮代码

- [x] Story / Process 页 hero 图：图片顶到最上、铺到导航栏下（消除页面跳动）
  - 去掉 hero 容器的 `padding-top`（含手机端 72px），图片从最顶端开始、由固定导航栏覆盖顶部
  - 撤销上一版「导航隐藏时收 padding」的做法（那会导致导航一隐藏整页跳一下）
  - 涉及 `story.html .story-hero`、`process.html .process-hero-media`

- [x] 修复首页 hero 视频「底部缺一条浅色带、未铺满」
  - 根因：服务流程板块 `.process-image` 改为 `position:absolute; top:-104px` 上探 104px，且 `.service-process` 为 `overflow:visible` + `z-index:2`，导致该图顶部盖住 hero 底部
  - 修复：`.service-process` 的 `overflow: visible` → `hidden`，裁掉上探部分；hero 恢复满屏，服务流程板块铺图不受影响（仅改一行）

## 当前状态

- 首页 Works 区：静态画廊布局（左大图 + 右 2×2），3 真实 + 2 占位，配色未变
- 项目图片来源：`images/<项目名>/精选卡/`（数据不再走 JS 数组，改为 HTML 内静态引用）
- 首页 hero 视频已恢复满屏；箭头设计标准已入档 `docs/design-spec.md`
- Story / Process 页 hero 图已改为「顶到最上、导航覆盖顶部、滚动不跳」

## 后续

- [ ] Works 两个占位项目（Project 04 / 05）待用户替换：改 `href`、`<img src>`、标题与年份，并删除 `works-card--placeholder` 类
- [ ] 若左大图底边与右下小图底边仍未完全齐平，可改用「右侧网格固定行高」方式强制对齐
- [ ] Works 图片间距（当前 44px）与列比例待用户确认观感后微调
