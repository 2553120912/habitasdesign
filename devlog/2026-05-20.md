# 2026-05-20 开发日志

## 今日完成

### 阶段一 — 基础搭建
- [x] 创建 `index.html` — 单页网站初版
  - Hero 首屏、作品展示、关于我们、联系方式四大板块
  - 极简黑白灰设计，衬线字体
  - 响应式布局（桌面 + 手机）
  - 固定导航 + 平滑滚动
- [x] 建立 `docs/` 规范文档体系（requirements / tech-spec / design-spec / workflow）
- [x] 建立 `devlog/` 开发日志文件夹
- [x] 创建 `CLAUDE.md` 项目指引
- [x] 添加中英文双语切换功能
  - 导航栏 `中 / EN` 切换按钮
  - JS 数据驱动翻译，`data-i18n` 属性绑定
  - localStorage 记住语言偏好
- [x] 导航栏改为始终深色实底 — 固定 `#1a1a1a` 背景 + 底部分隔线（删除了原本的滚动透明切换逻辑）

### 阶段二 — Logo 替换
- [x] 用真实事务所图标替换 `HB` 文字 logo
  - 文件保存在 `images/logo.png`（PNG 透明背景）
  - CSS `filter: invert(1)` 把黑色反转为白色，适配深色导航栏
  - Logo 包成 `<a>` 标签 → 点击回顶
  - Hover 时透明度变化

### 阶段三 — 真实信息替换（第一轮）
- [x] 读取用户提供的 `事务所信息.docx`（实际是邮件签名模板）
  - 发现事务所真名：**Habitas Design**（logo 的 H 即此名首字母）
  - 双办公点：墨尔本 + 上海
  - Director: Jonathan Xu
- [x] 全站 `HB` → `Habitas Design`
  - `<title>` / Hero 大标题 / logo alt+aria-label / 页脚版权 / JS 翻译字典
  - 页脚版权年份 `2024 → 2026`
- [x] Contact 区改造为**双办公点并列**布局
  - 墨尔本：U2 5 Aristoc Road, Glen Waverley, VIC, Australia（中英文都保留英文）
  - 上海：苏州南路 1295 号（中文）/ 1295 Suzhou South Road, Shanghai, China（英文）
  - 两边各自带电话
  - 下方一行：Email (`studio@habitasdesign.com`) + Website (`habitasdesign.com`)
  - 邮箱 → `mailto:` 链接；网站 → `https://` 新标签页
  - 增删 i18n key：删除 4 个旧 key，新增 10 个 key

### 阶段五 — 侧栏磨砂玻璃 + 修复 X 按钮被遮挡
- [x] 侧栏背景 `#1a1a1a` → `rgba(30, 30, 30, 0.75)`
- [x] 侧栏新增 `backdrop-filter: blur(12px)` + `-webkit-backdrop-filter` Safari 兼容前缀
  - 磨砂玻璃质感，背后内容隐约透出
- [x] 修复 X 按钮被侧栏遮挡的 bug
  - 原因：导航栏与侧栏 z-index 都是 100，DOM 顺序导致侧栏盖住汉堡按钮
  - 解决：`.nav` z-index `100 → 110`，让汉堡 → X 浮在侧栏之上
  - CSS 里 `汉堡 → X` 的变换（早已写好）现在能正确显示
- [x] 侧栏遮罩保留 `rgba(0,0,0,0.4)` 不动

### 阶段四 — 导航栏重构 + 微调
- [x] 三个链接（作品 / 关于 / 联系）**移出导航栏**，放入侧栏抽屉
  - 从右侧滑出的 340px 面板，手机端全宽
  - 半透明黑色遮罩 (rgba(0,0,0,0.4))
  - 三种关闭方式：点 ✕、点遮罩、按 ESC，链接点击后自动关闭并跳转
  - `body.menu-open` 锁定页面滚动
- [x] 汉堡按钮（`☰ ↔ ✕`）放在导航栏右侧
  - 纯 CSS：3 个 `<span>` + `transform: rotate()` 实现，无 SVG / 图标库
- [x] 中/EN 语言切换从右侧**移到 logo 旁边**（左侧成组）
- [x] Logo 放大：桌面 `36 → 44px`，手机 `28 → 32px`
- [x] `scroll-padding-top: 80 → 100px`（适应新导航栏高度）
- [x] 中 / EN 按钮**视觉等高**优化
  - 固定 `height: 20px` + `line-height: 1` + `inline-flex` 居中
  - 光学补偿：`中` 保持 12px，`EN` 加大到 14px（弥补 CJK 与拉丁 cap-height 差异）
- [x] Logo 与 lang-switch 间距加大：`24 → 40px`（手机 `16 → 24px`）

## 待办事项

### 内容缺失（影响真实感，建议下一步处理）
- [ ] **Hero 标语 / Tagline** 仍是占位文字（"空间为生活而存在 / 设计为体验而思考"）
- [ ] **About 区两段介绍**仍是占位文字（其中还提到 "HB 建筑事务所成立于 2018 年"，需替换）
- [ ] **作品卡片**仍是占位 — 山水居 / 城市绿洲 / 光之展厅（虚构项目）
  - 需真实项目名 + 年份 + 类型
  - 需真实项目图片（占位灰块 `#e8e6e1`）
  - 决定是否做项目详情子页面

### 已完成（来自 docx + 用户口述）
- [x] 收集事务所基础信息（名称 / 双办公地址 / 电话 / 邮箱 / 网站）

### 低优先级（待用户决定）
- [ ] 是否需要项目详情子页面（点击作品卡片进入）
- [ ] 是否需要团队介绍 / 新闻动态板块

## 备注

- **项目主体身份升级**：从 `HB 建筑事务所`（占位名）→ `Habitas Design`（真实名）。事务所位于澳大利亚墨尔本 + 中国上海，Director 是 Jonathan Xu
- **双语工作流反向**：常规中国事务所网站是 CN 为原文 + EN 翻译；本项目相反 —— 用户提供英文原稿（来自邮件签名等），Claude 翻译为中文，再双向写入 i18n 字典。中文页面上事务所名也保留英文 `Habitas Design`（用户偏好）
- **导航风格定调"克制式"**：默认导航栏只有 logo + 中/EN + 汉堡 三件套，链接收入侧栏。参考 SANAA / 隈研吾事务所网站的极简范式
- **技术栈维持纯粹**：仍是零依赖单文件 HTML/CSS/JS，无任何外部库（无 jQuery、无 SVG 图标库、无动画库、无 CSS 框架）
- **Plan Mode 用得多**：每个非微小改动都走 Plan Mode 写计划 → 用户批准 → 实施 → 浏览器验证。整天累计批准了 6 个独立 plan（logo / 内容更新 / 导航栏重构 / 中-EN 对齐微调 / 侧栏磨砂玻璃 等）
- **诊断 vs 新增**：阶段五的 X 按钮问题不是"功能缺失"而是"被盖住"——CSS 变换早就写好了。读代码先判断现状，再决定改什么，避免重复造轮子
